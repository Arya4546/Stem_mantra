import FloatingIcons from "@/components/animations/FloatingIcons";

export default function SectionFloatingIcons({ count = 2, zIndex = 1 }) {
  return <FloatingIcons count={count} zIndex={zIndex} />;
}
