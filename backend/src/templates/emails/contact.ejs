<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Contact Inquiry - STEMmantra</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f7fa;
      line-height: 1.6;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #ffffff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    }
    .header {
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      padding: 40px 30px;
      text-align: center;
    }
    .logo {
      color: #ffffff;
      font-size: 28px;
      font-weight: 700;
      letter-spacing: 1px;
    }
    .logo span {
      color: #FFC107;
    }
    .header-badge {
      display: inline-block;
      background-color: rgba(255, 255, 255, 0.2);
      color: #ffffff;
      padding: 8px 20px;
      border-radius: 30px;
      font-size: 14px;
      margin-top: 15px;
      font-weight: 500;
    }
    .alert-section {
      background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%);
      padding: 25px 30px;
      text-align: center;
    }
    .alert-icon {
      font-size: 40px;
      margin-bottom: 10px;
    }
    .alert-title {
      font-size: 22px;
      color: #92400e;
      font-weight: 600;
    }
    .alert-subtitle {
      color: #a16207;
      font-size: 14px;
      margin-top: 5px;
    }
    .content {
      padding: 40px 30px;
    }
    .section-title {
      font-size: 18px;
      color: #1a1a2e;
      margin-bottom: 20px;
      font-weight: 600;
      display: flex;
      align-items: center;
    }
    .section-title span {
      margin-right: 10px;
    }
    .info-card {
      background-color: #f8fafc;
      border-radius: 12px;
      padding: 25px;
      margin-bottom: 25px;
    }
    .info-row {
      display: flex;
      margin-bottom: 15px;
      align-items: flex-start;
    }
    .info-row:last-child {
      margin-bottom: 0;
    }
    .info-label {
      width: 120px;
      font-size: 12px;
      color: #64748b;
      text-transform: uppercase;
      letter-spacing: 1px;
      padding-top: 3px;
      flex-shrink: 0;
    }
    .info-value {
      flex: 1;
      font-size: 15px;
      color: #1a1a2e;
      font-weight: 500;
    }
    .info-value a {
      color: #0066FF;
      text-decoration: none;
    }
    .message-box {
      background-color: #ffffff;
      border: 1px solid #e2e8f0;
      border-radius: 12px;
      padding: 25px;
      margin: 25px 0;
    }
    .message-header {
      display: flex;
      align-items: center;
      margin-bottom: 15px;
      padding-bottom: 15px;
      border-bottom: 1px solid #e2e8f0;
    }
    .message-avatar {
      width: 50px;
      height: 50px;
      background: linear-gradient(135deg, #0066FF 0%, #4F46E5 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
      font-weight: 700;
      color: white;
      margin-right: 15px;
    }
    .message-meta h3 {
      font-size: 16px;
      color: #1a1a2e;
      margin-bottom: 3px;
    }
    .message-meta p {
      font-size: 13px;
      color: #64748b;
    }
    .message-content {
      color: #4a5568;
      font-size: 15px;
      line-height: 1.8;
      white-space: pre-wrap;
    }
    .inquiry-type {
      display: inline-block;
      padding: 6px 15px;
      border-radius: 20px;
      font-size: 13px;
      font-weight: 500;
      margin-top: 15px;
    }
    .inquiry-type.general {
      background-color: #e0e7ff;
      color: #4338ca;
    }
    .inquiry-type.demo {
      background-color: #dcfce7;
      color: #16a34a;
    }
    .inquiry-type.partnership {
      background-color: #fef3c7;
      color: #d97706;
    }
    .inquiry-type.support {
      background-color: #fee2e2;
      color: #dc2626;
    }
    .inquiry-type.other {
      background-color: #f3e8ff;
      color: #9333ea;
    }
    .action-buttons {
      display: flex;
      gap: 15px;
      margin-top: 30px;
    }
    .action-button {
      flex: 1;
      text-align: center;
      padding: 15px 25px;
      border-radius: 10px;
      text-decoration: none;
      font-size: 14px;
      font-weight: 600;
    }
    .action-button.primary {
      background: linear-gradient(135deg, #0066FF 0%, #4F46E5 100%);
      color: #ffffff;
    }
    .action-button.secondary {
      background-color: #f1f5f9;
      color: #475569;
    }
    .priority-indicator {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 15px;
      border-radius: 10px;
      margin: 25px 0;
    }
    .priority-indicator.high {
      background-color: #fee2e2;
      color: #dc2626;
    }
    .priority-indicator.medium {
      background-color: #fef3c7;
      color: #d97706;
    }
    .priority-indicator.low {
      background-color: #dcfce7;
      color: #16a34a;
    }
    .divider {
      height: 1px;
      background: linear-gradient(to right, transparent, #e2e8f0, transparent);
      margin: 30px 0;
    }
    .timestamp {
      text-align: center;
      color: #64748b;
      font-size: 13px;
    }
    .footer {
      background-color: #1a1a2e;
      padding: 25px;
      text-align: center;
    }
    .footer-logo {
      color: #ffffff;
      font-size: 18px;
      font-weight: 700;
      margin-bottom: 10px;
    }
    .footer-logo span {
      color: #FFC107;
    }
    .footer-text {
      color: rgba(255, 255, 255, 0.6);
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">STEM <span>Mantra</span></div>
      <div class="header-badge">📬 New Inquiry Received</div>
    </div>
    
    <div class="alert-section">
      <div class="alert-icon">🔔</div>
      <h1 class="alert-title">New Contact Form Submission</h1>
      <p class="alert-subtitle">A visitor has submitted an inquiry through the website</p>
    </div>
    
    <div class="content">
      <h2 class="section-title"><span>👤</span> Contact Information</h2>
      
      <div class="info-card">
        <div class="info-row">
          <div class="info-label">Name</div>
          <div class="info-value"><%= name %></div>
        </div>
        <div class="info-row">
          <div class="info-label">Email</div>
          <div class="info-value"><a href="mailto:<%= email %>"><%= email %></a></div>
        </div>
        <% if (phone) { %>
        <div class="info-row">
          <div class="info-label">Phone</div>
          <div class="info-value"><a href="tel:<%= phone %>"><%= phone %></a></div>
        </div>
        <% } %>
        <% if (organization) { %>
        <div class="info-row">
          <div class="info-label">Organization</div>
          <div class="info-value"><%= organization %></div>
        </div>
        <% } %>
        <% if (subject) { %>
        <div class="info-row">
          <div class="info-label">Subject</div>
          <div class="info-value"><%= subject %></div>
        </div>
        <% } %>
        <% if (inquiryType) { %>
        <div class="info-row">
          <div class="info-label">Inquiry Type</div>
          <div class="info-value">
            <span class="inquiry-type <%= inquiryType.toLowerCase() %>"><%= inquiryType %></span>
          </div>
        </div>
        <% } %>
      </div>
      
      <h2 class="section-title"><span>💬</span> Message</h2>
      
      <div class="message-box">
        <div class="message-header">
          <div class="message-avatar"><%= name.charAt(0).toUpperCase() %></div>
          <div class="message-meta">
            <h3><%= name %></h3>
            <p><%= email %></p>
          </div>
        </div>
        <div class="message-content"><%= message %></div>
      </div>
      
      <div class="action-buttons">
        <a href="mailto:<%= email %>?subject=Re: <%= subject || 'Your Inquiry at STEMmantra' %>" class="action-button primary">
          📧 Reply via Email
        </a>
        <% if (phone) { %>
        <a href="tel:<%= phone %>" class="action-button secondary">
          📞 Call Now
        </a>
        <% } %>
      </div>
      
      <div class="divider"></div>
      
      <div class="timestamp">
        📅 Received on <%= new Date().toLocaleString('en-IN', { 
          weekday: 'long', 
          year: 'numeric', 
          month: 'long', 
          day: 'numeric',
          hour: '2-digit',
          minute: '2-digit',
          timeZone: 'Asia/Kolkata'
        }) %> IST
      </div>
    </div>
    
    <div class="footer">
      <div class="footer-logo">STEM <span>Mantra</span></div>
      <div class="footer-text">
        This is an automated notification from your website's contact form.
      </div>
    </div>
  </div>
</body>
</html>
